# Script for populating the database. Run with: mix run priv/repo/seeds.exs
#
# Demo-farm seed. Phase 1 will replace the stub below with real calls
# to Peggy.Accounts.register_user/1 and Peggy.Farms.create_farm/2 once
# those contexts land.

require Logger

Logger.info("Dev seed stub — no schemas yet. Populate in Phase 1.")
